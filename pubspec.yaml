name: data_models
description: Package containing core elements of all apps developed within 
  the company. Designed for internal use only.
version: 1.1.0
homepage: https://github.com/NewGymBuddyApp/data_models
repository: https://github.com/NewGymBuddyApp/data_models
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  uuid: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

# The following section is specific to Flutter.
flutter:
